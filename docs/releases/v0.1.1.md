# Arch v0.1.1 Release Notes (2026-03-05)

Patch release for `arch.gocools` focused on discovery safety, graph diff performance, and lifecycle guardrails.

## Highlights

- Hardened AWS discovery retries and pagination handling with bounded backoff and pagination safety metrics.
- Reduced graph diff allocations for large snapshots and added stress/benchmark coverage.
- Enforced owner-tag validation for update and destroy lifecycle operations (including dry-run behavior).
- Enabled release workflow artifact upload for cross-platform binaries.

## Closed Issues

- #11 Add discovery backoff and pagination safeguards
- #12 Reduce graph diff memory overhead for large architectures
- #13 Enforce owner-tag validation for update and destroy operations

## Notable Commits

- `31abec7` discovery retry/pagination hardening
- `34ab639` graph diff allocation optimization
- `e7b625f` owner-tag enforcement on lifecycle operations
- `faab6f7` release workflow asset publishing
